<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:tx="http://www.springframework.org/schema/tx" xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
       http://www.springframework.org/schema/tx
       http://www.springframework.org/schema/tx/spring-tx-3.0.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context-3.0.xsd">

  <context:annotation-config/>

  <!-- Spring + MyBatis + DBCP -->
  <bean id="dataSource" class="org.apache.commons.dbcp.BasicDataSource" destroy-method="close">
    <!-- Connection Info -->
    <property name="driverClassName" value="${jdbc.driver}"/>
    <property name="url" value="${jdbc.url}"/>
    <property name="username" value="${jdbc.username}"/>
    <property name="password" value="${jdbc.password}"/>

    <!-- Connection Pooling DBCP -->
    <property name="validationQuery" value="SELECT 1"/>
    <property name="initialSize" value="${dbcp.initialSize}"/>
    <property name="maxActive" value="${dbcp.maxActive}"/>
    <property name="maxIdle" value="${dbcp.maxIdle}"/>
    <property name="maxWait" value="${dbcp.maxWait}"/>
    <property name="poolPreparedStatements" value="${dbcp.poolPreparedStatements}"/>
    <property name="defaultAutoCommit" value="${dbcp.defaultAutoCommit}"/>
  </bean>

  <bean id="sqlSessionFactory" class="com.dhf.boss.common.spring.ExtSqlSessionFactoryBean">
    <property name="dataSource" ref="dataSource"/>
    <property name="mapperLocations" value="classpath*:com/domain/java/dao/base/*/*.xml"/>
    <property name="extPackages" value="com.domain.java.dao.base.ext"/>
  </bean>

  <bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
    <property name="dataSource" ref="dataSource"/>
  </bean>

  <tx:annotation-driven transaction-manager="transactionManager"/>

  <bean id="read_dataSource" class="org.apache.commons.dbcp.BasicDataSource" destroy-method="close">
    <!-- Connection Info -->
    <property name="driverClassName" value="${jdbc.driver}"/>
    <property name="url" value="${read.jdbc.url}"/>
    <property name="username" value="${read.jdbc.username}"/>
    <property name="password" value="${read.jdbc.password}"/>

    <!-- Connection Pooling DBCP -->
    <property name="validationQuery" value="SELECT 1"/>
    <property name="initialSize" value="${dbcp.initialSize}"/>
    <property name="maxActive" value="${dbcp.maxActive}"/>
    <property name="maxIdle" value="${dbcp.maxIdle}"/>
    <property name="maxWait" value="${dbcp.maxWait}"/>
    <property name="poolPreparedStatements" value="${dbcp.poolPreparedStatements}"/>
    <property name="defaultAutoCommit" value="${dbcp.defaultAutoCommit}"/>
  </bean>

  <bean id="read_sqlSessionFactory" class="com.dhf.boss.common.spring.ExtSqlSessionFactoryBean">
    <property name="dataSource" ref="read_dataSource"/>
    <property name="mapperLocations" value="classpath*:com/domain/java/dao/base/*/*.xml"/>
    <property name="extPackages" value="com.domain.java.dao.base.ext"/>
  </bean>

  <bean id="read_transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
    <property name="dataSource" ref="read_dataSource"/>
  </bean>
  <tx:annotation-driven transaction-manager="read_transactionManager"/>

  <bean class="com.dhf.boss.common.spring.ProxyMapperScannerConfigurer">
    <property name="basePackage" value="com.domain.java.dao.base.ext"/>
    <property name="writeSqlSessionFactoryBeanName" value="sqlSessionFactory"/>
    <property name="readSqlSessionFactoryBeanName" value="read_sqlSessionFactory"/>
    <property name="writePatterns" value="update,delete,insert,save,modify,del"/>
    <property name="readPatterns" value="select,get,count,query"/>
  </bean>

</beans>